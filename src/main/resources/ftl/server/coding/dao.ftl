package ${clazz.pkg!}.service.${clazz.flName!}.dao;

import org.apache.ibatis.annotations.Mapper;

import ${clazz.pkg!}.service.${clazz.flName!}.${clazz.fuName!};
import cn.nineox.shuqitong.common.framework.base.IOxDao;

/**
/**
 * <p>
 * ${clazz.comment!} dao
 * </p>
 *
 * @author luxh
 * @since ${clazz.date!}
 */
 
@Mapper
public interface ${clazz.fuName!}Dao extends IOxDao<${clazz.fuName!}> {
	
}
