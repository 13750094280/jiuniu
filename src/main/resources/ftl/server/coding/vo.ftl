package ${clazz.pkg!}.web.controller.${clazz.flName!};

import java.util.List;


import ${clazz.pkg!}.service.${clazz.flName!}.${clazz.fuName!};
import lombok.Data;

/**
 * <p>
 * ${clazz.comment!} 
 * </p>
 *
 * @author luxh
 * @since ${clazz.date!}
 */
 
 
@Data
public class ${clazz.fuName!}VO extends ${clazz.fuName!} {

}
